<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.gdgmisantla.listaspersonalizadas" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:screenOrientation = "portrait"
            android:name="net.gdgmisantla.listaspersonalizadas.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:screenOrientation = "portrait"
            android:name="net.gdgmisantla.listaspersonalizadas.InformacionActivity"
            android:label="@string/informacion" >
        </activity>
        <activity
            android:screenOrientation = "portrait"
            android:name="net.gdgmisantla.listaspersonalizadas.PlantillaActivity"
            android:label="@string/titleplantilla" >
        </activity>
    </application>

</manifest>
